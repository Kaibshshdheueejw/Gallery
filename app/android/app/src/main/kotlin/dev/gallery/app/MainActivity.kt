package dev.gallery.app

import io.flutter.embedding.android.FlutterFragmentActivity

// FlutterFragmentActivity (not FlutterActivity) is required for the
// local_auth biometric prompt used by the private folder (Stage 2).
class MainActivity : FlutterFragmentActivity()
