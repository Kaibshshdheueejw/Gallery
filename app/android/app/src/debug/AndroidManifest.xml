<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Hot reload / DevTools need loopback networking in debug only. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
